export class Curso{
  nome: string;
  descricao: string;
  imagem: string;

  constructor(nome: string, descricao: string, imagem: string ){
    this.nome = nome;
    this.descricao = descricao;
    this.imagem = imagem;
  }
}
